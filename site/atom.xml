---
layout: compressxml
title: "Atom Feed"
permalink: /site/atom.xml
---
<?xml version="1.0" encoding="utf-8"?>
<?xml-stylesheet type="text/xsl" href="{{ site.baseurl }}/assets/xslt/atom.xslt" ?>
<?xml-stylesheet type="text/css" href="{{ site.baseurl }}/assets/css/atom.css" ?>
<feed xmlns="http://www.w3.org/2005/Atom">
	<id>{{ site.url }}{{ site.baseurl }}/</id>
	<title>{{ site.title | xml_escape }}</title>
	<updated>{{ site.time | date_to_xmlschema }}</updated>
	<subtitle>{{ site.description | xml_escape }}</subtitle>

	{% if site.author %}
		{% assign author = site.data.authors[site.author] %}
		<author>
			{% if author.full_name %}
				<name>{{ author.full_name | xml_escape }}</name>
			{% elsif author.name %}
				<name>{{ author.name | xml_escape }}</name>
			{% else %}
				<name>{{ site.author | xml_escape }}</name>
			{% endif %}
			
			{% if author.email %}
				<email>{{ author.email | xml_escape }}</email>
			{% endif %}
			
			{% if author.site %}
				<uri>{{ author.site | xml_escape }}</uri>
			{% endif %}
		</author>
	{% endif %}

	<link href="{{ site.url }}{{ site.baseurl }}{{ page.url }}" rel="self" type="application/rss+xml" />
	<link href="{{ site.url }}{{ site.baseurl }}/" rel="alternate" type="text/html" />

	{% for post in site.posts limit:37 %}
		<entry>
			<id>{{ site.url }}{{ site.baseurl }}{{ post.url }}</id>
			<title>{{ post.title | strip_html | strip_newlines | xml_escape }}</title>
			<link href="{{ site.url }}{{ site.baseurl }}{{ post.url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
			<updated>{{ post.date | date_to_xmlschema }}</updated>

			{% if post.author %}
				{% assign author = site.data.authors[post.author] %}
				<author>
					{% if author.name %}
						<name>{{ author.name | xml_escape }}</name>
					{% else %}
						<name>{{ post.author | xml_escape }}</name>
					{% endif %}
					{% if author.email %}
						<email>{{ author.email | xml_escape }}</email>
					{% endif %}
					{% if author.site %}
						<uri>{{ author.site | xml_escape }}</uri>
					{% endif %}
				</author>
			{% endif %}
			
			<summary>{% if post.summary %}{{ post.summary | xml_escape }}{% elsif post.description %}{{ post.description | xml_escape }}{% else %}{{ post.excerpt | strip_html | xml_escape }}{% endif %}</summary>
			<content type="text/html" src="{{ site.url }}{{ site.baseurl }}{{ post.url }}" />

			{% for category in post.categories %}
				<category term="{{ category | xml_escape }}" />
			{% endfor %}
			{% for tag in post.tags %}
				<category term="{{ tag | xml_escape }}" />
			{% endfor %}

			<published>{{ post.date | date_to_xmlschema }}</published>
		</entry>
	{% endfor %}

</feed>
