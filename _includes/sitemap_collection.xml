{% for link in include.links %}
  {% unless link.sitemap.exclude or link.published == false or link.sitemap == false or link.layout == 'null' or link.layout == 'none' %}{% if link.title %}
    {% assign pathext = link.path | split:'.' %}{% assign ext = pathext.last %}
    {% if ext == 'md' or ext == 'html' %}
      <url>
        <loc>{{ site.url }}{{ site.baseurl }}{{ link.url | remove: 'index.html' }}</loc>
        <layout>{{ link.layout }}</layout>
        {% if link.sitemap.lastmod %}<lastmod>{{ link.sitemap.lastmod | date: '%Y-%m-%d' }}</lastmod>{% elsif link.date %}<lastmod>{{ link.date | date_to_xmlschema }}</lastmod>{% else %}<lastmod>{{ site.time | date_to_xmlschema }}</lastmod>{% endif %}
        {% if link.sitemap.changefreq %}<changefreq>{{ link.sitemap.changefreq }}</changefreq>{% elsif include.changefreq %}<changefreq>{{ include.changefreq }}</changefreq>{% endif %}
        {% if link.sitemap.priority %}<priority>{{ link.sitemap.priority }}</priority>{% elsif include.priority %}<priority>{{ include.priority }}</priority>{% endif %}
      </url>
    {% endif %}
  {% endif %}{% endunless %}
{% endfor %}
